language: node_js
node_js:
  - "8"
  - "6"
  - "5"

sudo: true

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq -y nodejs build-essential
  - gem install fpm

install:
  - yarn

script:
  - yarn build
  - yarn electron-pack
